name: example
description: A starting point for Dart libraries or applications.
publish_to: none
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  carburetor: ^0.0.1

dev_dependencies:
  lints: ^5.0.0
  build_runner: ^2.3.0
  carburetor_build: ^0.0.1
  test: ^1.27.0
